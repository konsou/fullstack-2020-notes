{
  "name": "notes-backend",
  "version": "1.0.0",
  "description": "Notes with a backend service",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "Remove-Item .\\build\\ -Recurse;cd ..\\fullstack-2020\\osa3\\notes-spa-frontend\\;npm run build --prod;Copy-Item .\\build\\ ..\\..\\..\\fullstack-2020-notes\\ -Recurse",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui;git add .;git commit -m ui-build;git push;npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "Tomi Javanainen",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
